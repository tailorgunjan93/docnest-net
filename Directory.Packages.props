<Project>

  <!-- Central Package Management — every external version is pinned here (NFR: pinned deps). -->
  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    <CentralPackageTransitivePinningEnabled>true</CentralPackageTransitivePinningEnabled>
  </PropertyGroup>

  <!-- Runtime dependencies (kept minimal; each sits behind a DocNest wrapper). -->
  <ItemGroup>
    <!-- HTML parsing (DocNest.Parsers / HtmlParser only). MIT, W3C-compliant HTML5 parser. -->
    <PackageVersion Include="AngleSharp" Version="1.1.2" />
  </ItemGroup>

  <!-- Test-only dependencies. The core libraries have ZERO runtime package dependencies. -->
  <ItemGroup>
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageVersion Include="xunit" Version="2.9.2" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="2.8.2" />
    <!-- FluentAssertions pinned to the last Apache-2.0 (free) release; 7.x+ is commercial. -->
    <PackageVersion Include="FluentAssertions" Version="6.12.0" />
    <!-- Runtime-conditional [SkippableFact] — lets interop tests skip (not silently pass) when a
         fixture or Python is unavailable. -->
    <PackageVersion Include="Xunit.SkippableFact" Version="1.4.13" />
  </ItemGroup>

</Project>
